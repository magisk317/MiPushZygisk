# MiPush Zygisk device override config
#
# The built-in profile selected in app.conf supplies the complete device
# baseline. Keep this file empty unless you need explicit overrides.
#
# Optional global overrides:
# [system]
# ro.product.model=Some Other Model
#
# Optional per-package overrides:
# [com.example.app:system]
# ro.product.model=Some Other Model
