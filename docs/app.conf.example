# Enable all processes of a package:
com.example.app

# Optional settings:
profile=miui14
observe=false

# Enable only one process:
com.example.app|com.example.app:push

# Syntactically valid vendor and system packages can be configured explicitly.
# Only the exact android package and malformed package names are ignored.
